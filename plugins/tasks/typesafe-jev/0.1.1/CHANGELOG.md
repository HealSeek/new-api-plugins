---
changelogVersion: 1
plugin: "typesafe-jev"
version: "0.1.1"
locale: "en"
---

# Changelog

## [0.1.1]

### Changed

- Change the text-avatar fallback from `TS` to `TA` so it identifies TypeSafe AI rather than TypeScript when the sidecar logo cannot be loaded.
